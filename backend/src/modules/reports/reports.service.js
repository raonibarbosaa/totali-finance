// Stub temporário — Etapa 6 (Relatórios DRE/DFC) pendente de implementação.
// O backend service foi parcialmente gerado mas não foi concluído.
// Quando a etapa 6 for finalizada, este arquivo deve ser substituído
// pela implementação real (cálculo de DRE por regime, DFC, comparativo mensal).

const NOT_IMPLEMENTED_MSG = 'Etapa 6 (Relatórios DRE/DFC) pendente de implementação.';

async function getDRE(tenantId, params) {
  throw new Error(NOT_IMPLEMENTED_MSG);
}

async function getDFC(tenantId, params) {
  throw new Error(NOT_IMPLEMENTED_MSG);
}

async function getMonthlyComparison(tenantId, params) {
  throw new Error(NOT_IMPLEMENTED_MSG);
}

module.exports = { getDRE, getDFC, getMonthlyComparison };
